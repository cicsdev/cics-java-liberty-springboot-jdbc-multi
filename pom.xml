<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Inherit defaults from Spring Boot Parent -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.ibm.cicsdev.springboot</groupId>
  <artifactId>cics-java-liberty-springboot-jdbc-multi</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>
  <name>CICS JDBC Multi DataSource Spring Boot</name>
  <description>Spring Boot JDBC application using multiple DataSources for CICS Liberty JVM server</description>

  <properties>
    <java.version>17</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <cics.jvmserver>DFHWLP</cics.jvmserver>
    <!-- CICS TS BOM version — canonical property name used by both Maven and Gradle -->
    <cics.bom.version>6.1-20250812133513-PH63856</cics.bom.version>
  </properties>

  <modules>
    <module>cics-java-liberty-springboot-jdbc-multi-app</module>
    <module>cics-java-liberty-springboot-jdbc-multi-cicsbundle</module>
  </modules>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <release>${java.version}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
